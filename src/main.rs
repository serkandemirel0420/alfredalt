fn main() {
    println!("Rust backend is active.");
    println!("Use the native SwiftUI frontend in `swift/App/`.");
    println!("Generate bridge files with `./scripts/generate_swift_bridge.sh`.");
}
