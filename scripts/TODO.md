- sometimes setting stays behind serach bar
- update needs to run multiples

- delted files needs to be send to deleted folder, can be recoverable
- check for updates does not work, i have to click appearance tab or other tab then it finds new version.
- install new version does have to work two time, it restarts and ido install and restart again then works, current number changes
